We show implement drag drop support implement cut , copy , paste support . How combine one component ? You implement within TransferHandler 's importData method , like : ( transferSupport.isDrop ( ) ) { // put data transferSupport.getDropLocation ( ) } else { // determine want paste go ( ex : current selection ) // put data } The ListCutPaste example , discuss CCP non-Text Component page , support dnd ccp . Here importData method ( if-else drop logic bolded ) : public boolean importData ( TransferHandler.TransferSupport info ) { String data = null ; //If handle import , bail . ( ! canImport ( info ) ) { return false ; } JList list = ( JList ) info.getComponent ( ) ; DefaultListModel model = ( DefaultListModel ) list.getModel ( ) ; //Fetch data -- bail fail try { data = ( String ) info.getTransferable ( ) .getTransferData ( DataFlavor.stringFlavor ) ; } catch ( UnsupportedFlavorException ufe ) { System.out.println ( `` importData : unsupported data flavor '' ) ; return false ; } catch ( IOException ioe ) { System.out.println ( `` importData : I/O exception '' ) ; return false ; } ( info.isDrop ( ) ) { //This drop JList.DropLocation dl = ( JList.DropLocation ) info.getDropLocation ( ) ; int index = dl.getIndex ( ) ; ( dl.isInsert ( ) ) { model.add ( index , data ) ; return true ; } else { model.set ( index , data ) ; return true ; } } else { //This paste int index = list.getSelectedIndex ( ) ; // valid selection , // insert data selection ( index > = 0 ) { model.add ( list.getSelectedIndex ( ) +1 , data ) ; // else append end list } else { model.addElement ( data ) ; } return true ; } } This place need install if-else logic distinguish dnd ccp .