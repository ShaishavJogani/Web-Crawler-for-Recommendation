You categorize character accord property . For instance , X uppercase letter 4 decimal digit . Checking character property common way verify data enter end user . If sell book online , example , order entry screen verify character quantity field digit . Developers n't use write global software might determine character 's property compare character constant . For instance , might write code like : char ch ; // ... // This code WRONG ! // check ch letter ( ( ch > = 'a ' & & ch < = 'z ' ) || ( ch > = 'A ' & & ch < = 'Z ' ) ) // ... // check ch digit ( ch > = '0 ' & & ch < = '9 ' ) // ... // check ch whitespace ( ( ch == ' ' ) || ( ch =='\n ' ) || ( ch == '\t ' ) ) // ... The precede code wrong work English language . To internationalize previous example , replace following statement : char ch ; // ... // This code OK ! ( Character.isLetter ( ch ) ) // ... ( Character.isDigit ( ch ) ) // ... ( Character.isSpaceChar ( ch ) ) // ... The Character method rely Unicode Standard determine property character . Unicode 16-bit character encode support world 's major language . In Java programming language char value represent Unicode character . If check property char appropriate Character method , code work major language . For example , Character.isLetter method return true character letter Chinese , German , Arabic , another language . The following list give useful Character comparison method . The Character API documentation fully specify method . isDigit isLetter isLetterOrDigit isLowerCase isUpperCase isSpaceChar isDefined The Character.getType method return Unicode category character . Each category correspond constant define Character class . For instance , getType return Character.UPPERCASE_LETTER constant character A . For complete list category constant return getType , see Character API documentation . The following example show use getType Character category constant . All expression statement true : ( Character.getType ( 'a ' ) == Character.LOWERCASE_LETTER ) // ... ( Character.getType ( 'R ' ) == Character.UPPERCASE_LETTER ) // ... ( Character.getType ( ' > ' ) == Character.MATH_SYMBOL ) // ... ( Character.getType ( '_ ' ) == Character.CONNECTOR_PUNCTUATION ) // ...