To declare class implement interface , include implement clause class declaration . Your class implement one interface , implement keyword follow comma-separated list interface implement class . By convention , implement clause follow extends clause , one . A Sample Interface , Relatable Consider interface define compare size object . public interface Relatable { // ( object call isLargerThan ) // must instance // class return 1 , 0 , -1 // great , // equal , less public int isLargerThan ( Relatable ) ; } If want able compare size similar object , matter , class instantiate implement Relatable . Any class implement Relatable way compare relative `` size '' object instantiate class . For string , could number character ; book , could number page ; student , could weight ; forth . For planar geometric object , area would good choice ( see RectanglePlus class follow ) , volume would work three-dimensional geometric object . All class implement isLargerThan ( ) method . If know class implement Relatable , know compare size object instantiate class . Implementing Relatable Interface Here Rectangle class present Creating Objects section , rewrite implement Relatable . public class RectanglePlus implement Relatable { public int width = 0 ; public int height = 0 ; public Point origin ; // four constructor public RectanglePlus ( ) { origin = new Point ( 0 , 0 ) ; } public RectanglePlus ( Point p ) { origin = p ; } public RectanglePlus ( int w , int h ) { origin = new Point ( 0 , 0 ) ; width = w ; height = h ; } public RectanglePlus ( Point p , int w , int h ) { origin = p ; width = w ; height = h ; } // method move rectangle public void move ( int x , int ) { origin.x = x ; origin.y = ; } // method compute // area rectangle public int getArea ( ) { return width * height ; } // method require implement // Relatable interface public int isLargerThan ( Relatable ) { RectanglePlus otherRect = ( RectanglePlus ) ; ( this.getArea ( ) < otherRect.getArea ( ) ) return -1 ; else ( this.getArea ( ) > otherRect.getArea ( ) ) return 1 ; else return 0 ; } } Because RectanglePlus implement Relatable , size two RectanglePlus object compare . Note : The isLargerThan method , define Relatable interface , take object type Relatable . The line code , show bold previous example , cast RectanglePlus instance . Type casting tell compiler object really . Invoking getArea directly instance ( other.getArea ( ) ) would fail compile compiler understand actually instance RectanglePlus .