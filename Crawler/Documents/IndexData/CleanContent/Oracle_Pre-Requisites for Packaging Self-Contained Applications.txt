The Java Development Kit ( JDK ) require compile package application . The installable bundle must create platform self-contained application run . For example , application run Windows Linux , must run packaging tool Windows create .exe .msi bundle , run packaging tool Linux create .rpm .deb file . Third party tool require create installable bundle . The follow table identifies tool supported platform . Platform Format Tool Windows EXE Inno Setup 5 later Windows MSI WiX Toolset 3.8 later Linux RPM RPMBuild Linux DEB Debian packaging tool OS X DMG OS X PKG