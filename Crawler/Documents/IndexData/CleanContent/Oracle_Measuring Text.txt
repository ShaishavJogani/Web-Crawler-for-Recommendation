To properly measure text , need learn method mistake avoid . Font metric measurement text render Font object height line text font . The common way measure text use FontMetrics instance encapsulate metric information . For example : // get metric graphic FontMetrics metric = graphics.getFontMetrics ( font ) ; // get height line text // font render context int hgt = metrics.getHeight ( ) ; // get advance text font // render context int adv = metrics.stringWidth ( text ) ; // calculate size box hold // text padding . Dimension size = new Dimension ( adv+2 , hgt+2 ) ; This way sufficient many application evenly space line text size Swing component . Note following : The metric obtain Graphics class , class encapsulate FontRenderContext , need accurately measure text . At screen resolution , font adjust ease reading . As text size increase , adjustment scale linearly . So , 20 pt , font display text exactly twice length would 10 pt . Besides text font , important piece information need measure text FontRenderContext . This method include transform user space device pixel use measure text . The height report without reference particular string text . It useful , example , text editor want line space line text . stringWidth ( ) return advance width text . Advance width distance origin text position subsequently render string . When use method measure text , note text extend direction outside rectangle , define font height advance string . Typically , simple solution ensure text clip , example , component surround text . Add pad case text might otherwise clip . If solution insufficient , text measurement APIs Java 2D software return rectangular bounding box . These box account height specific text measure pixelization effect .