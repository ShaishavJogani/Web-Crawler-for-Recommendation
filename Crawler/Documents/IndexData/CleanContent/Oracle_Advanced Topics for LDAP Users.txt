The lesson LDAP trail provide detail mapping LDAP JNDI . They also give hint tip access LDAP service JNDI . LDAP X.500 , CCITT standard directory service part OSI suite service . X.500 standard define protocol ( among others ) client application access X.500 directory call Directory Access Protocol ( DAP ) . It layer top Open Systems Interconnection ( OSI ) protocol stack . The Internet community , recognize need X.500-like service face different underlying network infrastructure ( TCP/IP instead OSI ) , design new protocol base X.500 DAP protocol , call Lightweight DAP , LDAP . RFC 2251 defines call version 3 LDAP ( LDAP v3 ) , improved version predecessor LDAP v2 specify RFC 1777 . The goal LDAP protocol could easily implement , special focus able build small simple client . One way attempt achieve simplification use lot string minimize wherever possible use structure . DNs , example , represent protocol string , attribute type name many attribute value . The protocol consist client 's send request server , server respond , though necessarily order request send . Each request tag ID request response match . The protocol work either TCP UDP , although TCP version commonly use . Because focus client , LDAP community also define standard string representation DNs ( RFC 2553 ) , search filter ( RFC 1960 ) , attribute syntax ( RFC 1778 ) , C language base API ( RFC 1823 ) , format URLs access LDAP service ( RFC 1959 ) . LDAP v3 support internationalization , various authentication mechanism , referral , generic deployment mechanism . It allow new feature add protocol without also require change protocol use extension control .